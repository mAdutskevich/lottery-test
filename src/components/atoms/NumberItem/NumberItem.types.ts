export type TNumberItemProps = {
  text?: string;
  onPress?: () => void;
  noBorder?: boolean;
};

export type TPressableStyled = {
  noBorder?: boolean;
};

export type TInnerBorderContainerStyled = {
  noBorder?: boolean;
};
